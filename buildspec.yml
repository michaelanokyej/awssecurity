version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=javaprojectaws-ma -Dsonar.organization=javaprojectaws-ma -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=5ff940f6d539daba47c9972f9deed7eb2dbc5c49